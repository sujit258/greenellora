/**
 * Shared runtime constants used across both the Node.js API layer
 * and the Edge-compatible middleware. Keep this file free of any
 * Node-only or browser-only imports so it can be safely imported
 * from either environment.
 */

export const COOKIE_NAME = "admin_token";
